name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first greetings
        run: echo "helo world"
      - name: multiline
         run: |
              echo "line1"
              echo "line1"
